<div class="container p-4">
    <div class="row">
        {{#each linksTemas}}
           <div class="col-md-3">
               <div class="card text-center">
                   <div class="card-body">
                       <a href="#">
                           <h3 class="card-title text-uppercase">
                               {{title}}
                           </h3>
                       </a>
                       <p class="m-2">{{ description }}</p>
                       <p>{{timeago created_at}}</p>
                       <a href="/links/delete/{{id}}" class="btn btn-danger">Delete</a>
                       <a href="/links/edit/{{id}}" class="btn btn-secondary">Editar</a>
                   </div>
               </div>
           </div>
           {{else}}
           <div class="col-md-4 mx-auto">
               <div class="card card-body text-center">
                   <p class="display-4 mb-4">No hay Temas guardados aún</p>
                   <a href="/links/add" class="btn btn-primary stretched-link">CREA UNO!</a>
               </div>
           </div>
        {{/each}}
    </div>
</div>